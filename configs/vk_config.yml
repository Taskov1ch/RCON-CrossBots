# Will the bot work on this platform?
enabled: True

allowed_ids: # List of allowed users or communities (their IDs)
  - 614763437
  - -224130500

# (the token is specified in .env)

# Prefix for command execution
prefix: "/"
