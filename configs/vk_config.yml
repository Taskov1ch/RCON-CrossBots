# Будет ли работать бот на данной платформе?
enabled: True

allowed_ids: # Список разрешенных пользователей или сообществ (их ID)
  - 614763437
  - -224130500

# (токен указывается в .env)

# Префикс для выполнения команды
prefix: "/"